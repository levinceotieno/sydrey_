<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment Successful - Sydrey Enterprise</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <%- include('partials/header') %>
  
  <main class="container mx-auto mt-8 p-4">
    <div class="bg-white shadow-md rounded-lg p-6 max-w-lg mx-auto">
      <div class="text-center">
        <h1 class="text-2xl font-bold text-green-600 mb-4">Payment Successful!</h1>
        <div class="mb-6">
          <svg class="w-16 h-16 text-green-500 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
          </svg>
        </div>
        
        <p class="mb-2">Your payment of <span class="font-bold">KES <%= amount.toFixed(2) %></span> was successful.</p>
        <p class="mb-2">Order #: <span class="font-bold"><%= orderId %></span></p>
        <p class="mb-4 text-gray-600">Reference: <%= reference %></p>
        
        <div class="mb-6 text-left border-t border-b py-4 my-4">
          <h2 class="font-bold mb-2">Order Details:</h2>
          <p><strong>Delivery Address:</strong> <%= order.delivery_address %></p>
          <p><strong>Delivery Location:</strong> <%= order.delivery_location === 'withinKenya' ? 'Within Kenya' : 'Outside Kenya' %></p>
          <p><strong>Expected Delivery:</strong> <%= new Date(order.delivery_date).toLocaleDateString() %></p>
          <p><strong>Order Status:</strong> <span class="bg-yellow-100 text-yellow-800 px-2 py-1 rounded"><%= order.status %></span></p>
        </div>
        
        <p class="mb-6">Thank you for your order! We'll process it right away.</p>
        
        <div class="flex justify-center space-x-4">
          <a href="/orders/history" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">View My Orders</a>
          <a href="/" class="bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600">Back to Home</a>
        </div>
      </div>
    </div>
  </main>
  
  <%- include('partials/footer') %>
</body>
</html>
